:root{
  --max-width: 1200px;
  --accent-color: #d4af37;
  --light-accent: #ffefbb;
}

#concerts_section.concerts-layout{
  max-width: var(--max-width);
  margin: 0 auto;
  padding: 2rem 1rem 3rem;
}

/* Header: stile tipografico ereditato da base.css (coerente con Bio) */
#concerts_section .page-header{ margin-bottom: 1rem; }
#concerts_section .page-title{ margin:0 0 .5rem 0; }
#concerts_section .page-claim{ margin:0 0 1rem 0; }

/* Layout 2/3 sx + 1/3 dx */
#concerts_section .layout-grid{
  display:grid;
  grid-template-columns: 2fr 1fr;
  gap:1.25rem;
  align-items:start;
}
#concerts_section .left-col{ min-width:0; }

/* HERO destra sticky (controlli da JSON via style inline) */
#concerts_section .right-hero{
  position: sticky;
  top: 90px;
  min-height: var(--concert-hero-min-h, 520px);
  align-self:start;
  overflow:hidden;
  border-radius:14px;
  background:#000;
  box-shadow:0 6px 22px rgba(0,0,0,.45);
}
#concerts_section .hero-media{ position:relative; height:100%; }
#concerts_section .hero-media-img{
  width:100%; height:100%;
  object-fit:cover;
  object-position:center var(--concert-hero-focus-y, 50%);
  transform: scale(var(--concert-hero-zoom, 1));
  display:block;
  filter: brightness(.95);
}

/* Sezioni */
.section-title{ margin: 1rem 0 .6rem; }

/* FUTURI */
.upcoming-table{ display:grid; gap:.75rem; }
.up-row{
  display:grid;
  grid-template-columns:120px 1fr auto;
  align-items:center;
  gap:1rem;
  background:#121212;
  border:1px solid #222;
  border-radius:12px;
  padding:.85rem 1rem;
}
.up-date{ text-align:center; border-right:1px solid #222; padding-right:.75rem; }
.up-month{ font-weight:600; }
.up-day{ font-size:1.6rem; line-height:1; }
.up-title{ margin:0; font-size:1.05rem; }
.up-place, .up-line{ opacity:.9; }
.up-cta .btn{ white-space:nowrap; }

/* PAST – pulsantoni anni */
.year-pills{
  display:grid;
  grid-template-columns:repeat(auto-fill, minmax(120px, 1fr));
  gap:.6rem;
  margin:.6rem 0 1rem;
}
.year-pill{
  display:inline-block; width:100%;
  padding:.7rem .9rem;
  border:1px solid #303030; border-radius:12px;
  background:#161616; color:#fff; text-align:center;
  font-weight:600; letter-spacing:.02em; cursor:pointer;
  transition:transform .15s ease, background .15s ease, border-color .15s ease;
}
.year-pill:hover{ background:#1c1c1c; border-color:#3a3a3a; }
.year-pill.active{ background:#222; border-color:var(--accent-color); color:var(--light-accent); }

/* Contenuto anno (nascosto/mostrato via JS) */
#year_contents .year-content{ margin-top:.5rem; }
#year_contents .year-content[hidden]{ display:none; }

.concerts-year-list{ list-style:none; padding:0; margin:0; }
.concert-block{
  display:flex; gap:1rem;
  background:#1a1a1a; border:1px solid #222; border-left:4px solid var(--accent-color);
  border-radius:10px; padding:.85rem; margin-bottom:1rem;
}
.concert-poster{
  width:120px; max-height:180px; height:auto;
  object-fit:contain; background:#222;
  border-radius:6px; box-shadow:0 0 6px rgba(0,0,0,.18);
  cursor:pointer;
}
.concert-info{ flex:1 1 auto; }
.concert-title{ margin:0 0 .25rem 0; }
.concert-date, .concert-place{ margin:0; opacity:.95; }
.concert-details{ margin-top:.5rem; }
.details-summary{ cursor:pointer; }
.concert-actions{ margin-top:.4rem; }
.empty-state, .empty-year{ opacity:.8; }

/* Lightbox (nascondi di default quando presente in DOM) */
#concert_lightbox[hidden]{ display:none; }

/* Responsive */
@media (max-width: 980px){
  #concerts_section .layout-grid{ grid-template-columns:1fr; }
  #concerts_section .right-hero{ position:relative; top:0; order:-1; }
  .up-row{ grid-template-columns:100px 1fr; }
  .up-cta{ grid-column:1 / -1; justify-self:end; }
}